# Agent instructions

使用中文回复。

## Agent skills

### Issue tracker

需求、PRD、Wayfinder 地图和 tickets 使用 GitHub Issues。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五角色标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单上下文结构：根目录 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
